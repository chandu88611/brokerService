# brokerApiService

Centralized broker API service (India brokers) with adapter-based design.

## Scripts
- `npm run dev` - start dev server
- `npm run build` - build TypeScript
- `npm start` - run built server

## Env
Copy `.env.example` to `.env` and fill values.
